apply plugin: 'application'

run {
    main = 'axon.data.AxonDataApplication'
    args 'server', 'data.yml'
}

dependencies {
    compile project(':axon-data-api')
    compile project(':axon-shared')

    compile 'io.dropwizard:dropwizard-core:1.1.0'
}
